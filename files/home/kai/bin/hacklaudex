#!/usr/bin/env bash

set -Eeo pipefail
trap 'echo "exit $?: $BASH_COMMAND(line $LINENO)" >&2' ERR

unset ANTHROPIC_API_KEY
export CLAUDE_CONFIG_DIR="/home/kai/.config/claudex/config"
exec claude "$@"
