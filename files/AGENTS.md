<!-- Generated by sync-agent-files.sh. DO NOT EDIT. -->

## .claude/rules/files.md

Directory scope: `files/**`


* Symlink source files managed by `setup.sh` - changes here require `setup.sh` updates
* `setup.sh` creates symlinks from `files/{target}` to `{target}` (e.g., `files/home/kai/.gitconfig` → `/home/kai/.gitconfig`)
* Adding, renaming, or deleting files MUST update `_USER_TARGETS` or `_SYSTEM_TARGETS` array in `setup.sh` - `_USER_TARGETS` for a target under `files/home/kai/`, whose entry is written relative to it and symlinked into every user in `_USERS`, `_SYSTEM_TARGETS` for anything else under `files/`, whose entry is the absolute destination; the bullets below carve out the exemptions
* Files under `files/` are symlinked into every user in `_USERS` and must stay world-readable; the claudex sandbox runs with `UMask=0077` and `setup.sh` only `chown`s, never `chmod`s, so set `umask 022` in any script that generates into `files/`
* Commit a new script under `files/usr/local/bin/` or `files/home/kai/bin/` with mode 755 - the symlink `setup.sh` creates delivers the repository file's own mode, so a 644 script fails only when a keybind or unit invokes it, never during setup
* A `bin/sync-agent-files.sh` output a tool reads as its own home configuration belongs under `files/` and in `_USER_TARGETS`, or `_SYSTEM_TARGETS` when the target sits outside `files/home/kai/`, so it stays version-controlled; write straight into `~` only when the tool owns sibling content in the same directory (codex owns `~/.codex/skills/.system/`) and treat those destinations as not reproducible from the repository
* The directory-scoped `AGENTS.md` the same script derives from a rule's `paths` never gets its own entry in either array - its body cites repository-relative paths that no longer resolve from a symlink destination, and `files/AGENTS.md` would land at `/AGENTS.md`, which FHS does not allow; omitting the entry is not what keeps it out of `~` either, since a mirror inside an already-registered directory (`files/home/kai/.config/claudex/config/{agents,commands,hooks}/`) still arrives there through that directory's own symlink
* A `files/home/kai/` file no local consumer resolves from `~` gets no entry either - `files/home/kai/.config/claudex/config/CLAUDE.ci.md` is such a file, so a symlink into every user's home would deliver something nothing there reads
* Register the deepest path the repository fully owns, never a directory the tool also writes its own state into - `~/.codex/` holds auth, session and sqlite state beside the generated `agents/`, `prompts/` and `hooks.json`, so each of those children takes its own entry and the `## Workflow` table's `rm -rf` then reaches only what the repository can reproduce
* Compare a destination that already exists on the host against its `files/` source before adding its entry - `setup.sh` deletes a real file standing where a symlink must go and the `## Workflow` table's `rm -rf` does the same for a directory, so anything present only at the destination is gone with nothing to restore it from; port it into `files/` first or say that it is being dropped
* `tmux` subcommands acting on a pane or window resolve to the focused pane when `-t` is omitted or empty, so with several sessions sharing one tmux server they silently hit another session's pane - pass an explicit target unless the script builds a layout without capturing any pane id
* A script needing a git write the sandbox forbids - staging, committing, resolving a merge - must ask `claudex ... --print` for text only and run git itself on the host, never delegate the git command; a worktree created inside the repository keeps its real gitdir under the parent's `.git/worktrees/`, so it stays read-only there even though the worktree files are writable, and `commit.gpgsign` additionally needs the `~/.gnupg` that `files/home/kai/bin/claudex` marks inaccessible
* Count hunks with `git diff --no-textconv` wherever the count has to line up with what git itself walks - `.gitattributes` assigns a `diff=` driver to the `.enc` files, and a driver that changes the line count makes plain `git diff` report fewer hunks than `git add -p` prompts for, silently shifting every answer fed to it
* Read the user's command history from `~/.local/share/fish/fish_history`, never `~/.bash_history` - fish is the shell `files/home/kai/.tmux.conf` starts and bash's history carries no timestamp record at all, so no date window can be cut from it; anchor the timestamp match as `/^  when: /`, since an entry's optional `added_when:` line matches an unanchored `when:` and is then read as an execution time, and select the window with `awk` rather than reading the file, which reaches back years
* `files/usr/local/bin/backup.sh` snapshots `/` daily, and a btrfs snapshot leaves a nested subvolume as an empty stub while forcing CoW on everything else it captures - `chattr +C` included - so a host path a script fills with a large, continuously rewritten file has to be a subvolume of its own rather than merely NoCow, or each day's rewrites pin the previous day's blocks until the LVM thin pool runs out of data space (`files/usr/local/bin/minikube-start.sh:128`)

## Script Placement

| Script type | Location |
|-------------|----------|
| Project-dependent operations (repository management, CI/CD tasks) | `bin/` |
| Project-independent tools (system utilities, service integrations) | `files/usr/local/bin/` |

## Script Naming in `files/usr/local/bin/`

| Type | Naming | Example |
|------|--------|---------|
| Keybind-callable scripts | No extension, hyphen-separated | `screenshot`, `up-volume`, `toggle-easyeffects` |
| Utility scripts | `.sh` extension, hyphen-separated | `backup.sh`, `startup.sh`, `sync.sh` |

## Script Interface in `files/usr/local/bin/`

| Value | Form |
|-------|------|
| A tunable the script itself decides (retention count, poll interval, scan window) | UPPERCASE constant at the top |
| Data only the caller can supply (a Spotify URI) | Positional argument, parsed with the template in `.claude/reference/bash/argument-parsing.md` |

An openbox keybind (`files/home/kai/.config/openbox/rc.xml:155`) and a systemd unit (`files/etc/systemd/system/backup.service:5`) both invoke these with no arguments, so a flag added for a tunable has no caller left that could set it.

## Workflow

| Operation | `setup.sh` array | Suggest to user |
|-----------|------------------|-----------------|
| Add file | Add target path to `_USER_TARGETS` or `_SYSTEM_TARGETS` | `ln -s <source> <destination>` (per user in `_USERS` for `_USER_TARGETS`) |
| Add directory | Add target path to `_USER_TARGETS` or `_SYSTEM_TARGETS` | `rm -rf <destination>` + `ln -s <source> <destination>` (`setup.sh` only `rmdir`s, so a non-empty destination aborts it) |
| Rename file | Update entry | `rm <old_destination>` + `ln -s <new_source> <new_destination>` |
| Delete file | Remove entry | `rm <destination>` |

Source/destination follow `Directory Structure`.

## Directory Structure

| Path Pattern | Symlink Target |
|--------------|----------------|
| `files/home/kai/*` | `/home/kai/*` |
| `files/etc/*` | `/etc/*` |
| `files/usr/local/bin/*` | `/usr/local/bin/*` |
| `files/usr/share/*` | `/usr/share/*` |

## Reference

If calling `tmux` from a script:
  Read: `.claude/reference/files/tmux.md`
