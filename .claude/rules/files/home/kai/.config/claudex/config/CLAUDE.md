---
paths:
  - "files/home/kai/.config/claudex/config/CLAUDE*.md"
---

* Re-run `bin/sync-agent-files.sh` after editing `files/home/kai/.config/claudex/config/CLAUDE*.md` so the tracked `AGENTS.md` files under `files/home/kai/.codex/`, `files/home/kai/.gemini/`, and `files/home/kai/.config/opencode/` do not drift from their source
* Agent (`@name`), tool, or Claude Code-specific mechanism names (e.g. hook names) not matched by its removal filters leak verbatim into every tool's `AGENTS.md` - prefer tool-neutral wording and read the script's `# Global user prompt` section before adding them
* Its removal filters are whole-line `sed` deletes, so keep a filtered name on a line that carries nothing else — one embedded mid-paragraph drops every instruction sharing that line for the tools that filter it, alternatives named alongside it included; equally, a header line introducing a list leaves a dangling imperative when every item under it is filtered, so keep one unfiltered item under such a header or fold the header into the item, then grep each generated `AGENTS.md` for the instruction after re-running
* `bin/sync-agent-files.sh` appends a fixed `Git Usage Rules` block after the filtered body, so a statement about what the sandbox lets git write has to be reconciled with that block or every generated `AGENTS.md` contradicts itself
* The `CLAUDE*.md` files `CLAUDE.md` `@`-imports become a single prompt, so a rule added to one has to be checked against the closed lists and the standing obligations the others define - widening an exclusion in one file strands whatever another obliges the response to carry, and nothing compares the files at runtime
* `bin/sync-agent-files.sh` splices each `@`-import in place of its own line and every imported `CLAUDE*.md` opens with a `##` heading, so a paragraph written below one renders under that file's last section - keep an instruction that must stay top-level above the first `@`-import, and order the imports so nothing between them lands in a section it does not belong to
* Put a new rule in `CLAUDE.important.md` when it constrains the shape of a response or the steps a task must take, in `CLAUDE.summary.md` when it constrains the final summary, and in `CLAUDE.general.md` when it guides a judgement the agent would otherwise make freely - a rule constraining a structure one of them defines belongs beside that definition even when the thing it names is defined in another, and the glob's other two matches take no rule of their own: `CLAUDE.ci.md` holds nothing but `@`-imports, and `CLAUDE.md` only instructions naming the local MCP servers and tmux, which CI never provisions
* `CLAUDE.summary.md` fixes the final summary as a template that admits nothing but placeholder substitution, and one of its placeholders repeats the previous response verbatim, so any rule that forces new content into a response also lands there and is banned until the same edit adds both a section to its fenced block and that placeholder's description below it - widening the prose alone leaves the template contradicting it
* The findings section carries only what neither the repeated response nor the change itself already shows and what still needs a decision - that is why it lists deferred findings rather than resolved or declined ones, and the same test decides any section proposed for the template unless, like `{{ネクストアクション}}`, it reports the instruction's status rather than a finding
* `files/home/kai/.config/claudex/config/hooks/Stop.sh` cites `CLAUDE.summary.md` and its template rather than restating either, so editing the definition needs no matching edit there - renaming or moving that file does, since the hook names it by path
* CI reaches these files only through `CLAUDE.ci.md`, which every workflow running `anthropics/claude-code-action@v1` copies to `${{ runner.temp }}/claude-home/CLAUDE.md` alongside one `cp` line per `@`-imported file
* Adding or renaming a `CLAUDE*.md` `@`-import means an entry in `CLAUDE.md`, one in `CLAUDE.ci.md`, and a `cp` line in every such workflow, on top of the `_USER_TARGETS` entry `.claude/rules/files.md` requires - `CLAUDE.md` alone leaves the CI agent short of that content while local Claude Code still has it, and a `cp` line alone lands a file in `claude-home` that nothing imports
