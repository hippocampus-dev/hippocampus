apiVersion: v1
kind: Service
metadata:
  name: reporting-server
spec:
  selector:
    app.kubernetes.io/name: reporting-server
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
