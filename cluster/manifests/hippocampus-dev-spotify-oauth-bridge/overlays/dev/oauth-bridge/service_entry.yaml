apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: accounts.spotify.com
spec:
  exportTo:
    - .
  hosts:
    - accounts.spotify.com
  location: MESH_EXTERNAL
  ports:
    - name: https
      number: 443
      protocol: HTTPS
  resolution: DNS
