apiVersion: v1
kind: Namespace
metadata:
  name: elastic-system
  labels:
    name: elastic-system
    # elastic-cloud-kubernetes upstream lacks seccompProfile
    pod-security.kubernetes.io/enforce: baseline
