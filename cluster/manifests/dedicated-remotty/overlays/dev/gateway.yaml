apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: dedicated-remotty
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - dedicated-remotty.minikube.127.0.0.1.nip.io
        - dedicated-remotty.kaidotio.dev
      port:
        number: 80
        name: http
        protocol: HTTP
