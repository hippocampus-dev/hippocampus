apiVersion: apps/v1
kind: Deployment
metadata:
  name: at-least-semaphore-pod-hook
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 1
  template:
    metadata:
      labels:
        sidecar.istio.io/inject: "true"
      annotations:
        sidecar.istio.io/proxyCPULimit: 1000m
        sidecar.istio.io/proxyMemoryLimit: 1Gi
        sidecar.istio.io/proxyCPU: 30m
        sidecar.istio.io/proxyMemory: 64Mi
        prometheus.io/scrape: "true"
        prometheus.io/scheme: http
        prometheus.io/port: "8080"
        prometheus.io/path: /metrics
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          nodeAffinityPolicy: Honor
          nodeTaintsPolicy: Honor
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          matchLabelKeys:
            - pod-template-hash
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: at-least-semaphore-pod-hook
              app.kubernetes.io/component: ""
        - maxSkew: 1
          nodeAffinityPolicy: Honor
          nodeTaintsPolicy: Honor
          topologyKey: topology.kubernetes.io/zone
          #minDomains: 3
          #whenUnsatisfiable: DoNotSchedule
          whenUnsatisfiable: ScheduleAnyway
          matchLabelKeys:
            - pod-template-hash
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: at-least-semaphore-pod-hook
              app.kubernetes.io/component: ""
      containers:
        - name: hook
          args:
            - webhook
            - --redis-addresses=at-least-semaphore-pod-hook-redis-0.at-least-semaphore-pod-hook-redis.at-least-semaphore-pod-hook.svc.cluster.local:6379,at-least-semaphore-pod-hook-redis-1.at-least-semaphore-pod-hook-redis.at-least-semaphore-pod-hook.svc.cluster.local:6379,at-least-semaphore-pod-hook-redis-2.at-least-semaphore-pod-hook-redis.at-least-semaphore-pod-hook.svc.cluster.local:6379
            - --queue-redis-address=at-least-semaphore-pod-hook-redis-0.at-least-semaphore-pod-hook-redis.at-least-semaphore-pod-hook.svc.cluster.local:6379
            - --enable-sidecar-containers
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 1000m
              memory: 128Mi
