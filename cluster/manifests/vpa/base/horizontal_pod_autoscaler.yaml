apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vpa-admission-controller
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vpa-admission-controller
