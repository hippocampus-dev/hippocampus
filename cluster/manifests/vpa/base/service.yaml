apiVersion: v1
kind: Service
metadata:
  name: vpa-webhook
spec:
  selector:
    app.kubernetes.io/name: vpa
    app.kubernetes.io/component: admission-controller
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: https
