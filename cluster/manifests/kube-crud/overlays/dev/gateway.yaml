apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: kube-crud
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - kube-crud.kaidotio.dev
      port:
        number: 80
        name: http
        protocol: HTTP
