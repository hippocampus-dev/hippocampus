apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: kube-crud
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kube-crud
      app.kubernetes.io/component: ""
  mtls:
    mode: STRICT
