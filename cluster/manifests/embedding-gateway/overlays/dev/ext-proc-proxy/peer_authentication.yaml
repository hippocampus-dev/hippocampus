apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: ext-proc-proxy
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: ext-proc-proxy
  mtls:
    mode: STRICT
