apiVersion: v1
kind: Service
metadata:
  name: talk
spec:
  selector:
    app.kubernetes.io/name: talk
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
