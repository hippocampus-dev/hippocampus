apiVersion: v1
kind: Service
metadata:
  name: blackbox-exporter
spec:
  selector:
    app.kubernetes.io/name: blackbox-exporter
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 9115
      protocol: TCP
      targetPort: http