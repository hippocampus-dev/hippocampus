apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: bakery
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - bakery.minikube.127.0.0.1.nip.io
        - bakery.kaidotio.dev
      port:
        number: 80
        name: http
        protocol: HTTP
