apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: runner
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  # https://github.com/kubernetes/kubectl/issues/981
  name: cluster-admin
subjects:
  - kind: ServiceAccount
    name: runner
