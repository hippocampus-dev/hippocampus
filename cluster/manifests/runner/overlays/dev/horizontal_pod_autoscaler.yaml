apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: runner
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: runner
  maxReplicas: 3
  minReplicas: 1
  metrics:
    - type: External
      external:
        metric:
          name: github_actions_runs_queued
          selector:
            matchLabels:
              app_kubernetes_io_name: hippocampus
        target:
          type: Value
          value: "1"
    - type: External
      external:
        metric:
          name: github_actions_runners_busy
          selector:
            matchLabels:
              labels: github-actions-runner-controller
        target:
          type: Value
          value: "1"
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
