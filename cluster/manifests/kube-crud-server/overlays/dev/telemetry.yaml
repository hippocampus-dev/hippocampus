apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: kube-crud-server
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kube-crud-server
      app.kubernetes.io/component: ""
  tracing:
    - providers:
        - name: otel-agent
      randomSamplingPercentage: 100
  metrics:
    - providers:
        - name: prometheus
      reportingInterval: 15s
  accessLogging:
    - providers:
        - name: envoy
      filter:
        expression: request.protocol != null
      match:
        mode: SERVER
