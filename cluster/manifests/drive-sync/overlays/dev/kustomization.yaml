apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: drive-sync

resources:
  - namespace.yaml
  - network_policy.yaml
  - peer_authentication.yaml
  - sidecar.yaml
  - telemetry.yaml
  - minio
