apiVersion: v1
kind: Service
metadata:
  name: http-kvs
spec:
  selector:
    app.kubernetes.io/name: http-kvs
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
