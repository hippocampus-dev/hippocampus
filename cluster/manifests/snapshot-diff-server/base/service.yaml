apiVersion: v1
kind: Service
metadata:
  name: snapshot-diff-server
spec:
  selector:
    app.kubernetes.io/name: snapshot-diff-server
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
