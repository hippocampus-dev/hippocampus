apiVersion: v1
kind: Service
metadata:
  name: csviewer
spec:
  selector:
    app.kubernetes.io/name: csviewer
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
