apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: csviewer
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - csviewer.minikube.127.0.0.1.nip.io
        - csviewer.kaidotio.dev
      port:
        number: 80
        name: http
        protocol: HTTP
