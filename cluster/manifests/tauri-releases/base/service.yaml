apiVersion: v1
kind: Service
metadata:
  name: tauri-releases
spec:
  selector:
    app.kubernetes.io/name: tauri-releases
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
