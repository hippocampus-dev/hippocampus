apiVersion: networking.istio.io/v1
kind: Sidecar
metadata:
  name: redis
spec:
  workloadSelector:
    labels:
      app.kubernetes.io/component: redis
  outboundTrafficPolicy:
    mode: REGISTRY_ONLY
  egress:
    - captureMode: DEFAULT
      hosts:
        - ./dedicated-container-ingress-controller-redis.dedicated-container-ingress-controller.svc.cluster.local
        - istio-system/istiod.istio-system.svc.cluster.local
        - otel/otel-agent.otel.svc.cluster.local
---
apiVersion: networking.istio.io/v1
kind: Sidecar
metadata:
  name: redis-haproxy
spec:
  workloadSelector:
    labels:
      app.kubernetes.io/component: redis-haproxy
  outboundTrafficPolicy:
    mode: REGISTRY_ONLY
  egress:
    - captureMode: DEFAULT
      hosts:
        - ./dedicated-container-ingress-controller-redis.dedicated-container-ingress-controller.svc.cluster.local
        - istio-system/istiod.istio-system.svc.cluster.local
        - otel/otel-agent.otel.svc.cluster.local
