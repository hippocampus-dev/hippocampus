apiVersion: v1
kind: Service
metadata:
  name: device-flow-bridge
spec:
  selector:
    app.kubernetes.io/component: device-flow-bridge
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
