apiVersion: v1
kind: Service
metadata:
  name: oauth-bridge
spec:
  selector:
    app.kubernetes.io/component: oauth-bridge
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
