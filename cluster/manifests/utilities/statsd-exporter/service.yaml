apiVersion: v1
kind: Service
metadata:
  name: statsd
spec:
  selector:
    app.kubernetes.io/component: statsd-exporter
  ports:
    - name: tcp
      port: 9125
      protocol: TCP
      targetPort: tcp
    - name: udp
      port: 9125
      protocol: UDP
      targetPort: udp
