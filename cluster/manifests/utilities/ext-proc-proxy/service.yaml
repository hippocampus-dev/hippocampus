apiVersion: v1
kind: Service
metadata:
  name: ext-proc-proxy
spec:
  selector:
    app.kubernetes.io/component: ext-proc-proxy
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
