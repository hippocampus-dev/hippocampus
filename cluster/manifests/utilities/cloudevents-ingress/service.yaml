apiVersion: v1
kind: Service
metadata:
  name: cloudevents-ingress
spec:
  selector:
    app.kubernetes.io/component: cloudevents-ingress
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
