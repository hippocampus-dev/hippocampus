apiVersion: v1
kind: Service
metadata:
  name: http-redis-proxy
spec:
  selector:
    app.kubernetes.io/component: http-redis-proxy
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
