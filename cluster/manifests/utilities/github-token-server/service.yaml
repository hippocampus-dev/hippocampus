apiVersion: v1
kind: Service
metadata:
  name: github-token-server
spec:
  selector:
    app.kubernetes.io/component: github-token-server
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
