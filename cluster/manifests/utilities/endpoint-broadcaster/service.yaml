apiVersion: v1
kind: Service
metadata:
  name: endpoint-broadcaster
spec:
  selector:
    app.kubernetes.io/component: endpoint-broadcaster
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
