apiVersion: v1
kind: Service
metadata:
  name: lighthouse-exporter
spec:
  selector:
    app.kubernetes.io/name: lighthouse-exporter
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
