apiVersion: v1
kind: Namespace
metadata:
  name: slack-logger
  labels:
    name: slack-logger
    # slack-logger uses Strimzi Kafka (no RestrictedPodSecurityProvider)
    pod-security.kubernetes.io/enforce: baseline
