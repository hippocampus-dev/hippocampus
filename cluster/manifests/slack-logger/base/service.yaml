apiVersion: v1
kind: Service
metadata:
  name: slack-logger
spec:
  selector:
    app.kubernetes.io/name: slack-logger
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
