apiVersion: v1
kind: Namespace
metadata:
  name: fluentd-delayed-unlink
  labels:
    name: fluentd-delayed-unlink
    # fluentd-delayed-unlink uses eBPF with hostPID
    pod-security.kubernetes.io/enforce: privileged
