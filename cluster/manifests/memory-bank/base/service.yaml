apiVersion: v1
kind: Service
metadata:
  name: memory-bank
spec:
  selector:
    app.kubernetes.io/name: memory-bank
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
