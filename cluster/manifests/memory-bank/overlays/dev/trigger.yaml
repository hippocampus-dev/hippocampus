apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: cloudevents-relay
spec:
  broker: default
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: memory-bank-cloudevents-relay
