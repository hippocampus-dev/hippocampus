apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: cloudevents-relay
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudevents-relay
  mtls:
    mode: STRICT
