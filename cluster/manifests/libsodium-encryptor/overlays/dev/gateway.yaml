apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: libsodium-encryptor
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - libsodium-encryptor.minikube.127.0.0.1.nip.io
        - libsodium-encryptor.kaidotio.dev
      port:
        number: 80
        name: http
        protocol: HTTP
