apiVersion: v1
kind: Service
metadata:
  name: grafana
spec:
  selector:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: http
    - name: http-mcp
      port: 8000
      protocol: TCP
      targetPort: http-mcp
