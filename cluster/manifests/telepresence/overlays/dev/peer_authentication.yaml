apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: telepresence
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: telepresence
      app.kubernetes.io/component: ""
  mtls:
    mode: STRICT
  portLevelMtls:
    "8443":
      mode: PERMISSIVE # AdmissionWebhook from API server
