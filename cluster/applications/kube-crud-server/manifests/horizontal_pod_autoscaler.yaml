apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kube-crud-server
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kube-crud-server
  maxReplicas: 5
  minReplicas: 1
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
