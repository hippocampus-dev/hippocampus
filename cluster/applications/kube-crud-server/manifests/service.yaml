apiVersion: v1
kind: Service
metadata:
  name: kube-crud-server
spec:
  selector:
    app.kubernetes.io/name: kube-crud-server
    app.kubernetes.io/component: ""
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
