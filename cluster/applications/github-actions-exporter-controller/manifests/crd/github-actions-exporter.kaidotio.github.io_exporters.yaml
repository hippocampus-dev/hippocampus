---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: exporters.github-actions-exporter.kaidotio.github.io
spec:
  group: github-actions-exporter.kaidotio.github.io
  names:
    kind: Exporter
    listKind: ExporterList
    plural: exporters
    singular: exporter
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Exporter is the schema for the exporters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ExporterSpec defines the desired state of Exporter
              Scope is inferred from repo field: if repo is set, scope is repository; otherwise, scope is organization.
            properties:
              appSecretRef:
                description: |-
                  SecretEnvSource selects a Secret to populate the environment
                  variables with.

                  The contents of the target Secret's Data field will represent the
                  key-value pairs as environment variables.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret must be defined
                    type: boolean
                type: object
                x-kubernetes-map-type: atomic
              owner:
                description: Owner specifies the GitHub organization or user
                pattern: ^[^/]+$
                type: string
              repo:
                description: |-
                  Repo specifies the GitHub repository name. If set, metrics are collected at repository level.
                  If not set, metrics are collected at organization level.
                pattern: ^[^/]+$
                type: string
              template:
                description: Template defines the pod template generated by exporter
                properties:
                  metadata:
                    description: |-
                      Standard object's metadata.
                      More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              tokenSecretKeyRef:
                description: Selects a key of a GitHub Token secret in the exporter's
                  namespace
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
            required:
            - owner
            type: object
          status:
            description: ExporterStatus defines the observed state of Exporter
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
