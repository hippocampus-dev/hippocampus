[package]
name = "finalizer"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
proxy-wasm = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"]
