apiVersion: v1
kind: Service
metadata:
  name: lifecycle-job-hook
spec:
  selector:
    app.kubernetes.io/name: lifecycle-job-hook
    app.kubernetes.io/component: ""
  ports:
    - name: https
      port: 9443
      protocol: TCP
      targetPort: https
