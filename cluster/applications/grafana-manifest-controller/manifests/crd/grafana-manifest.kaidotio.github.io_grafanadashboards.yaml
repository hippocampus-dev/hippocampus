---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: grafanadashboards.grafana-manifest.kaidotio.github.io
spec:
  group: grafana-manifest.kaidotio.github.io
  names:
    kind: GrafanaDashboard
    listKind: GrafanaDashboardList
    plural: grafanadashboards
    singular: grafanadashboard
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.uid
      name: UID
      type: string
    - jsonPath: .status.version
      name: Version
      type: integer
    - jsonPath: .status.lastSyncedAt
      name: LastSynced
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: GrafanaDashboard is the Schema for the grafanadashboards API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GrafanaDashboardSpec defines the desired state of GrafanaDashboard
            properties:
              configMapRef:
                description: ConfigMapRef is a reference to a ConfigMap containing
                  the dashboard Jsonnet
                properties:
                  key:
                    description: The key to select.
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the ConfigMap or its key must be
                      defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              folder:
                description: Folder is the Grafana folder name to place the dashboard
                  in
                type: string
              jsonnet:
                description: Jsonnet is the dashboard definition in Jsonnet format
                type: string
            type: object
            x-kubernetes-validations:
            - message: one of jsonnet or configMapRef must be specified
              rule: has(self.jsonnet) || has(self.configMapRef)
          status:
            description: GrafanaDashboardStatus defines the observed state of GrafanaDashboard
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the dashboard's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncedAt:
                description: LastSyncedAt is the last time the dashboard was synced
                  to Grafana
                format: date-time
                type: string
              uid:
                description: UID is the Grafana dashboard UID
                type: string
              url:
                description: URL is the Grafana dashboard URL
                type: string
              version:
                description: Version is the Grafana dashboard version
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
