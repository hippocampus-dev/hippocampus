apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: monitoring
type: Opaque
stringData:
  # Option 1: API Key (Service Account Token)
  api-key: "glsa_xxxxxxxxxxxxxxxxxxxx"
  # Option 2: Basic Auth (uncomment if using basic auth instead)
  # username: "admin"
  # password: "admin"
