apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: skaffold-

namespace: skaffold-alerthandler

resources:
  - ../manifests
  - namespace.yaml

patches:
  - path: patches/service.yaml
