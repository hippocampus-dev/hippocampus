apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: persistentvolumeclaim-hook
spec:
  secretName: persistentvolumeclaim-hook-certs
  issuerRef:
    name: persistentvolumeclaim-hook-selfsigned