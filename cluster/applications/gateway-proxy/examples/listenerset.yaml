apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: example-listenerset
spec:
  parentRef:
    name: example-gateway
  listeners:
    - name: tcp-9000
      port: 9000
      protocol: TCP
