apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: distribution-scheduler
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: distribution-scheduler
      app.kubernetes.io/component: ""
