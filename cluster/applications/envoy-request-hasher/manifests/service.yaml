apiVersion: v1
kind: Service
metadata:
  name: envoy-request-hasher
spec:
  selector:
    app.kubernetes.io/name: envoy-request-hasher
    app.kubernetes.io/component: ""
  ports:
    - name: grpc
      port: 50051
      protocol: TCP
      targetPort: grpc
