apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: skaffold-

labels:
  - includeSelectors: true
    pairs:
      variant: skaffold

namespace: skaffold-owasp-zap-controller

resources:
  - ../manifests
  - namespace.yaml

patches:
  - path: patches/deployment.yaml
  # CRD does not support namePrefix
  - patch: |
      - op: replace
        path: /metadata/name
        value: scans.skaffold.owasp-zap.kaidotio.github.io
      - op: replace
        path: /spec/group
        value: skaffold.owasp-zap.kaidotio.github.io
    target:
      kind: CustomResourceDefinition
      name: scans.owasp-zap.kaidotio.github.io
  - patch: |
      - op: add
        path: /rules/0
        value:
          apiGroups:
            - skaffold.owasp-zap.kaidotio.github.io
          resources:
            - "*"
          verbs:
            - "*"
    target:
      kind: ClusterRole
      name: owasp-zap-controller
