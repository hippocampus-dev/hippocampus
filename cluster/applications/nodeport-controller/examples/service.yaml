apiVersion: v1
kind: Service
metadata:
  name: example
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: example
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
