apiVersion: v1
kind: Service
metadata:
  name: envoy-markdownify
spec:
  selector:
    app.kubernetes.io/name: envoy-markdownify
    app.kubernetes.io/component: ""
  ports:
    - name: grpc
      port: 50051
      protocol: TCP
      targetPort: grpc
