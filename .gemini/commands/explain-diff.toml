# Generated by sync-agent-files.sh. DO NOT EDIT.
description = "Explain working-tree changes using investigation notes from ~/brain/report/*/tasks/, falling back to past conversation logs"
prompt = """

Explain why each file in the current working tree was modified, by cross-referencing investigation notes under `~/brain/report/*/tasks/`. For files without a matching report, fall back to searching past Claude Code conversation logs via `/claude:logs`.

## Current changes

!`git status --porcelain`

## Available reports

!`ls -d ~/brain/report/*/tasks/ 2>/dev/null`

## Instructions

1. From the `git status` output above, collect every modified, added, deleted, or untracked path. Use absolute paths.

2. For each path, locate matching reports by searching both the absolute path and the basename:

   ```sh
   grep -ln -E "<absolute-path>|<basename>" ~/brain/report/*/tasks/*.md
   ```

3. For each matched report, `Read` it and find the section discussing that file (typically `## Investigated`, `## Decided`, or `## Changed`).

4. For each path with **no matching report**, invoke the `claude:logs` skill via the `Skill` tool with a query containing the path and basename to locate the relevant past conversation. Distill the reason from the most relevant log excerpt.

5. Distill a one-paragraph reason per file. Preserve both root-cause and fix when both appear.

6. When multiple sources (reports or logs) cite the same file, cite the most recent one first and note any earlier context.

## Output Format

For each modified file:

### `<absolute-path>`

- **Source**: `<absolute-path-to-report>` (date) — or `claude:logs` (session/date)
- **Reason**: <distilled summary>

List files with no source found in either reports or logs at the end under `## No source found`.
"""
