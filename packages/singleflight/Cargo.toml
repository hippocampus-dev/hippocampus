[package]
name = "singleflight"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.45.1", features = ["sync"] }

[dev-dependencies]
tokio = { version = "1.45.1", features = ["sync", "macros", "rt-multi-thread", "time"] }
