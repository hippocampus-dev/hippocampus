[package]
name = "hippocampus-configuration"
version = "0.1.0"
edition = "2021"

[dependencies]
error = { path = "../error" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[features]
default = []
sqlite = []
cassandra = []
gcs = []
wasm = []
