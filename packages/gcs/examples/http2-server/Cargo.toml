[package]
name = "http2-server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.10.0", features = ["macros", "rt-multi-thread"] }
hyper = { version = "0.14.11", features = ["server", "tcp", "http1", "http2"] }
